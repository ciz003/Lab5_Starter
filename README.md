# Lab 5 - Starter
<!-- Make sure you make a PR to your own repo's main and not the class' repo!! Otherwise you will lose points!! -->
Cindy Zhang
1) I would not use a unit test to test the "message" feature of a messaging application because it would not be sufficient enough to test its functionality. There are too many parts of sending a message (eg UI, backend, network to send the message, etc.) and too many variables for a unit test to be enough.

2) I would use a unit test to test the "max message length" feature of a messaging application because it is a small, isolated rule. Since it is only testing one feature, a unit test would be sufficient.